sudo: true
dist: trusty
jobs:
  include:
    - stage: build docker image
      script:
      - docker build -t test .
      - docker tag test chronomint
      - docker push gcr.io/forest/chronomint:latest
